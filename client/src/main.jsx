import React from "react";
import ReactDOM from "react-dom/client";
import RecordAudio from "./RecordAudio.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RecordAudio />
  </React.StrictMode>
);
